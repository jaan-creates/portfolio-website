# Selects which landing-page hero renders. Build-time only (Vite import.meta.env).
#   unset / anything else → default portrait + perspective-grid hero
#   "ai"                  → HeroAlt (AI-PM variant)
VITE_SITE_VARIANT=
